export const server = import.meta.env.VITE_SERVER;

//fix alert listener, add refetch listener
//something went wrong at the time of accepting freiend request
//no user info after login
//add loading & disable register button
